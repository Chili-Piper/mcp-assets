---
name: handoff-guide
description: Executes a live SDR-to-AE prospect handoff — initializes the session, selects a routing path, and books the meeting while the prospect is still on the call
version: 0.1.0-draft
inputs:
  - name: routing_id
    type: string
    description: "The routing ID for the handoff flow (obtain from the router configuration)"
    required: true
  - name: router_id
    type: string
    description: "The router ID to use; optional if the routing_id uniquely identifies the flow"
    required: false
  - name: path_id
    type: string
    description: "A specific routing path ID to pre-select; if omitted, available paths are presented for human selection"
    required: false
outputs:
  - name: handoff_session
    description: Initialized handoff session token and routing context
  - name: selected_path
    description: The selected routing path and available time slots
  - name: booked_meeting
    description: Confirmed meeting details after scheduling
tools_required: [chili-piper-mcp]
human_decision_point: "Review available routing paths and confirm the AE/path selection before the meeting is booked with the prospect"
writes_to: "Chili Piper handoff session and meeting record"
api_note: "Draft generated by edge-api-sync on 2026-05-28. Needs human review before use. handoff-select-simple added in DISTRO-4406 (merged 2026-05-27) — it is the selection step between handoff-init and handoff-schedule. Exact parameter shapes for handoff-init and handoff-schedule need to be confirmed from the edge-fire-service docs or live tool introspection before this skill goes live."
---

# Handoff Guide

<!-- DRAFT: Generated skeleton. Review and complete before publishing. -->

You are a RevOps automation agent. Your job is to execute a smooth live prospect handoff — initializing the session, selecting the right AE or routing path, and confirming the meeting booking while the prospect is still engaged.

## API reference

| Tool | What it returns |
|------|------------------|
| `handoff-init` | Initialize a handoff session → session token and available routing context |
| `handoff-select-simple` | Select a routing path for the handoff → available slots and path confirmation (added DISTRO-4406, 2026-05-27); args: `routingId` (req), `routerId` (opt), `pathId` (opt), `body` (opt) |
| `handoff-schedule` | Schedule the handoff meeting → confirmed meeting details |

---

## Step 1 — Initialize the handoff session

<!-- TODO: confirm exact parameter names for handoff-init from edge-fire-service docs or live tool schema -->

```
tool: handoff-init
args:
  # TODO: add required parameters (routingId? prospectEmail? meetingTypeId?)
```

Capture the session token or session ID from the response — it is required for the subsequent steps.

---

## Step 2 — Select the routing path

If `path_id` was provided by the human, pass it directly. Otherwise, let `handoff-select-simple` surface available options and present them for human selection.

```
tool: handoff-select-simple
args:
  routingId: <routing_id>
  routerId: <router_id — omit if not provided>
  pathId: <path_id — omit if not provided>
  body: <additional body params — TODO: confirm shape from docs>
```

**Human decision point**

*"Which routing path / AE should I assign this handoff to? Review the available options above and confirm before I book the meeting."*

---

## Step 3 — Schedule the handoff meeting

<!-- TODO: confirm exact parameter names for handoff-schedule — likely requires session token from Step 1 and selected path/slot from Step 2 -->

```
tool: handoff-schedule
args:
  # TODO: session token from Step 1
  # TODO: selected path/slot from Step 2
  # TODO: prospect contact details
```

---

## Step 4 — Output format

<!-- TODO: define output table based on actual handoff-schedule response shape -->

### Handoff Result

| Field | Value |
|-------|-------|
| Status | Booked / Failed |
| Meeting ID | |
| AE / Host | |
| Scheduled time | |
| Guest | |

**Human decision point**

*"The handoff is booked. Should I update the CRM record, or is there anything else to action for this prospect?"*

---

## Data handling

- **PII present:** yes — prospect contact info passed in booking request
- **Storage:** ephemeral
- **Writes:** Chili Piper handoff session and meeting record
